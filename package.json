{
  "name": "chaptale",
  "version": "1.5.0",
  "private": true,
  "description": "A cross-platform creative writing IDE for novels and scripts",
  "keywords": [
    "ai",
    "creative-writing",
    "ide",
    "novel",
    "script"
  ],
  "license": "MIT",
  "author": "yoyo514",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "run-p dev:app dev:api",
    "dev:app": "pnpm --filter @chaptale/app dev",
    "dev:api": "pnpm --filter @chaptale/api dev",
    "build": "turbo run build",
    "build:shared": "turbo run build --filter @chaptale/shared",
    "build:app": "turbo run build --filter @chaptale/app",
    "build:api": "turbo run build --filter @chaptale/api",
    "typecheck": "turbo run typecheck",
    "typecheck:shared": "turbo run typecheck --filter @chaptale/shared",
    "typecheck:app": "turbo run typecheck --filter @chaptale/app",
    "typecheck:api": "turbo run typecheck --filter @chaptale/api",
    "check": "run-s lint typecheck test",
    "test": "turbo run test",
    "test:watch": "vitest --config vitest.config.ts",
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "format": "oxfmt .",
    "format:check": "oxfmt . --check",
    "prepare": "husky",
    "preview": "pnpm --filter @chaptale/app preview"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "catalog:",
    "husky": "catalog:",
    "lint-staged": "catalog:",
    "npm-run-all": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "turbo": "catalog:",
    "vitest": "catalog:"
  },
  "lint-staged": {
    "**/*.{js,jsx,ts,tsx,json,jsonc,vue}": [
      "oxfmt"
    ],
    "**/*.{js,jsx,ts,tsx}": [
      "oxlint --fix"
    ]
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.9.0"
}
